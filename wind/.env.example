# API keys for the wind pipeline. Copy to .env (gitignored) before running.

# Inference (infer.py) — set the key for whichever backend you use.
OPENAI_API_KEY=
OPENROUTER_API_KEY=

# Teacher + synthetic data generation (teacher.py, generate_synthetic.py)
# both go through OpenRouter — covered by OPENROUTER_API_KEY above.

# Hugging Face Hub (ft/train.py, ft/quantize.py): model push + auth.
HF_TOKEN=
